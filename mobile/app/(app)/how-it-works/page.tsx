"use client";

import { HowItWorksContent } from "@/components/how-it-works-content";

/** In-app "How to use" tab. Guarded by the (app) RequireProfile layout. */
export default function HowItWorksPage() {
  return <HowItWorksContent />;
}
